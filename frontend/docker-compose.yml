services:
  frontend:
    build:
      context: .
      args:
        VITE_API_URL: http://localhost:3001/api
    container_name: spese-frontend
    ports:
      - "8080:80"
    restart: unless-stopped
